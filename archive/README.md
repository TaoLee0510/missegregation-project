# Archived material

This directory is not part of the executable pipeline.

- `reference/ALFA-K-source/`: preserved upstream source repository; active Salehi inputs were moved to `data/salehi_reference/`.
- `future/topology_forecastability/`: deferred protocol only; it has no runnable scripts.
- `../outputs/archive/`: prior local and exploratory run outputs.
